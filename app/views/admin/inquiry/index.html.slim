.container
  .row style="margin-top: 40px;"
    .col-xs-3
    .col-xs-6
      h2
        | 問い合わせ一覧
      table.table.table-bordered.table-hover.admin-table
        thead
          tr
            th
              | 問い合わせID
            th
              | 会員名
            th
              | 問い合わせタイトル
            th
              |

        tbody
          - @inquiries.each do |inquiry|
            tr
              td
                = inquiry.id
              td
                = Member.find_by(id: inquiry.member_id).name
              td
                = inquiry.title
              td.text-center
                = link_to '詳細', admin_inquiry_path(inquiry)